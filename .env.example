# Non-secret defaults. Copy to .env and fill secrets locally.
# Never commit .env.

WEB_PORT=3000
BACKEND_PORT=8080

INSTRUCTIONS_PATH=/instructions
DATA_PATH=/data
BACKEND_URL=http://backend:8080

INGEST_DPI=150
EMBEDDING_MODEL=histogram-hsv-256
TOP_K=12
SEARCH_HIST_WEIGHT=0.72
SEARCH_SILHOUETTE_WEIGHT=0.28
SEARCH_KIND_BOOST=0.03

# Secrets (leave empty unless you use an optional catalog snapshot)
# REBRICKABLE_API_KEY=
